
// List of vendors that should be excluded
export const excludedVendors = [
  "Johan von Lindeman", 
  "DFC Panama", 
  "Bottom Consulting",
  "Mr. Analytics LLC",
  // Add any other vendors that should be excluded here
];

// Cache duration in milliseconds (15 minutes)
export const CACHE_DURATION = 15 * 60 * 1000;

// Client-side cache key prefix
export const CLIENT_CACHE_KEY_PREFIX = "zoho_finance_data";
